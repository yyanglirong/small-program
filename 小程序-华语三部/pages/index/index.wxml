<view class="wrap">
        <view class="record" wx:for="{{music}}" wx:key="{{item}}"
         bind:tap="handleClick" data-id="{{item.id}}">
                <image src="{{item.img}}"/>
        <text>{{item.name}}</text>
      </view>
</view>
